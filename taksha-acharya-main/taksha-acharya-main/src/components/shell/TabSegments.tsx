// Segmented controls were used by the 4-tab shell to reveal sub-routes under
// Coach (Learn/Video) and Ask (Ask/Quiz/Apply). The 7-tab shell surfaces each
// destination directly, so this component is no longer needed.
// Keeping the file + null render so any stale import stays a no-op.
export default function TabSegments() {
  return null;
}
